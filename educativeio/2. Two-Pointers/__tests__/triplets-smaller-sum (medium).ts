const tripletsSmallerSum_test = require('../triplets-smaller-sum (medium)');

describe('tripletsSmallerSum_test', () => {
  it('should', () => {
    expect(tripletsSmallerSum_test([-1, 4, 2, 1, 3], 5)).toEqual(4);
  })
})