const squaringSortedArray_test = require('../squaring-sorted-array (easy)');

describe('squaringSortedArray_test', () => {
  it('should', () => {
    expect(squaringSortedArray_test([-2, -1, 0, 2, 3])).toEqual([0, 1, 4, 4, 9]);
  })
})