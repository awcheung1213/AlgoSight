const pairTargetSum_test = require('../pair-target-sum (easy)');

describe('pairTargetSum_test', () => {
  it('should', () => {
    expect(pairTargetSum_test([1, 2, 3, 4, 6], 6)).toEqual([1,3]);
  })
})