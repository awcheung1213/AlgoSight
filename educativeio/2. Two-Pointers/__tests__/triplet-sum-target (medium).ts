const tripletSumTarget_test = require('../triplet-sum-target (medium)');

describe('tripletSumTarget_test', () => {
  it('should', () => {
    expect(tripletSumTarget_test([-2, 0, 1, 2], 2)).toEqual(1);
  })
})