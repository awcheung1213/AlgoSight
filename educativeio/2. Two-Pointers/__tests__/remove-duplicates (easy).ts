const removeDuplicates_test = require('../remove-duplicates (easy)');

describe('removeDuplicates_test', () => {
  it('should', () => {
    expect(removeDuplicates_test([2, 3, 3, 3, 6, 9, 9])).toEqual(4);
  })
})