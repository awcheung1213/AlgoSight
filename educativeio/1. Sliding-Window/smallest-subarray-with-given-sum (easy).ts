//init smallestLength = 0
//init start
//init windowSum
//init for loop with end = 0
  //add value of index end to windowSum
  //if windowSum >= s
    //update smallestLength 
    //while windowSum >= s
      //subtract value of index start from windowSum
      //increment start

      longest-substring-with-distinct (medium).ts