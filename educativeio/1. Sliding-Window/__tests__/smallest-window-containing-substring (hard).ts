const smallestWindowSubstring_test = require('../smallest-window-containing-substring (hard)');

describe('smallestWindowSubstring_test', () => {
  it('should', () => {
    expect(smallestWindowSubstring_test('aabdec', 'abc')).toEqual('abdec')
  })
})