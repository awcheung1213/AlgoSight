const longestSubarrayReplace_test = require('../longest-subarray-replace (hard)');

describe('longestSubarrayReplace_test', () => {
  it('should', () => {
    expect(longestSubarrayReplace_test([0, 1, 1, 0, 0, 0, 1, 1, 0, 1, 1], 2)).toEqual(6)
  })
})