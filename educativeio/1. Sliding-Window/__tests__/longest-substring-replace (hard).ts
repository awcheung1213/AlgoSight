const longestSubstringReplace_test = require('../longest-substring-replace (hard)');

describe('longestSubstringReplace_test', () => {
  it('should', () => {
    expect(longestSubstringReplace_test("aabcdbb", 2)).toEqual(5)
  })
})