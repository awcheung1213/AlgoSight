const max_sum_subarray_test = require('../maximum-sum-subarray-size-K (easy)');

describe('max_sum_subarray_test', () => {
  it('should', () => {
    expect(max_sum_subarray_test([2, 1, 5, 1, 3, 2], 3)).toEqual(9)
  })
})