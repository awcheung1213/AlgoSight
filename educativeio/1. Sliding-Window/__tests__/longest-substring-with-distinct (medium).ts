const longest_substring_distinct_test = require('../longest-substring-with-distinct (medium)');

describe('longest_substring_distinct_test', () => {
  it('should', () => {
    expect(longest_substring_distinct_test("araaci", 2)).toEqual(4)
  })
})