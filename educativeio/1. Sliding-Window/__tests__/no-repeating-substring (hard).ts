const noRepeatingSubstring_test = require('../no-repeating-substring (hard)');

describe('noRepeatingSubstring_test', () => {
  it('should', () => {
    expect(noRepeatingSubstring_test('aabccbb')).toEqual(3)
  })
})