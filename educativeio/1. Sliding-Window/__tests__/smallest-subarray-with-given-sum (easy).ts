const smallest_subarray_test = require('../smallest-subarray-with-given-sum (easy)');

describe('smallest_subarray_test', () => {
  it('should', () => {
    expect(smallest_subarray_test([2, 1, 5, 2, 3, 2], 7)).toEqual(2)
  })
})